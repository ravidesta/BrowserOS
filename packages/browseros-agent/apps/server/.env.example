# Ports
BROWSEROS_CDP_PORT=9000
BROWSEROS_SERVER_PORT=9100
BROWSEROS_EXTENSION_PORT=9300

# Directories (optional - defaults to cwd)
# BROWSEROS_RESOURCES_DIR=./resources
# BROWSEROS_EXECUTION_DIR=./out

# VM cache (optional - runtime downloads published agent cache in background)
# Set prefetch=false to skip startup warmup; VM/OpenClaw startup still syncs on demand.
BROWSEROS_VM_CACHE_PREFETCH=true
BROWSEROS_VM_CACHE_MANIFEST_URL=https://cdn.browseros.com/vm/manifest.json

# BrowserOS config
BROWSEROS_CONFIG_URL=https://llm.browseros.com/api/browseros-server/config
BROWSEROS_VERSION=
BROWSEROS_INSTALL_ID=
BROWSEROS_CLIENT_ID=

# Graph service
CODEGEN_SERVICE_URL=

# Telemetry
POSTHOG_API_KEY=
SENTRY_DSN=

NODE_ENV=development
LOG_LEVEL=info

# Debug — captures every LLM call to .devtools/generations.json (view with `npx @ai-sdk/devtools`)
# BROWSEROS_AI_SDK_DEVTOOLS=true

# Testing
BROWSEROS_TEST_HEADLESS=false

# OnlyOffice Document Server (self-hosted) — powers the Office Suite app
# Set to the base URL of your OnlyOffice Document Server instance (e.g. https://docs.example.com)
ONLYOFFICE_DOCSERVER_URL=
# Optional: JWT secret if your Document Server has JWT validation enabled (recommended for production)
ONLYOFFICE_JWT_SECRET=
